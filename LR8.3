#define _CRT_SECURE_NO_WARNINGS
#include <iostream>

int main()
{
    setlocale(LC_ALL, "ru");
    int a, b, c;
    printf("Введите значения а и в: ");
    scanf("%d %d", &a, &b);
    if ((a>0) && (b>0) && (a>b))
    {
        while(a>b)
        {
            a = a - b;
        }
        printf("Остаток = %d.\n", a);
        return 0;
    }
    else
    {
        printf("Прочтите условие и повторите\n");
        return 0;
    }
}
