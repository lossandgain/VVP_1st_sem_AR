#include <iostream>
#include <cmath>
using namespace std;


int sign(float X) {
    if (X < 0) {
        return -1;
    }
    else if (X == 0) {
        return 0;
    }
    else if (X > 0) {
        return 1;
    }
}
int main() {
    setlocale(LC_ALL, "ru");
    float A, B, a, b;
    printf("Введите числа А и В для нахождения Sign(A) + Sign(B): ");
    cin >> A >> B;
    a = sign(A);
    b = sign(B);
    printf("Искомое значение равно: %g", a + b);
}
