#include <iostream>
using namespace std;


int main() {
    setlocale(LC_ALL, "ru");
    const int n = 10;
    if (n > 2) {
        int a, b, i, s, k;
        int arr[n];
        printf("Введите числа А и В: ");
        cin >> a >> b;
        arr[0] = a;
        arr[1] = b;
        s = a + b;
        for (i = 2; i < n; i++) {
            arr[i] = s;
            s += arr[i];
        }
        for (i = 0; i < n; i++)
            printf("%d ", arr[i]);
        return 0;
    }
    else printf("error");
    return 0;
}
